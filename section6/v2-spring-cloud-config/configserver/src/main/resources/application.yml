spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: "file:///Users//mahendrakundare//Documents//config"
        #search-locations: "classpath:/config"

server:
  port: 8071
